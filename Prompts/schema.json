{
  "type": "object",
  "properties": {
    "GrandTotal.Amount": {
      "type": ["number", ""],
      "description": "Total payable amount on the invoice"
    },
    "Invoice.Date": {
      "type": ["string", ""],
      "description": "Issue date of the invoice"
    },
    "Sender.VatId": {
      "type": ["string", ""],
      "description": "Tax ID of the sender (VAT)"
    },
    "Vat.Rate": {
      "type": ["number", ""],
      "description": "VAT percentage applied"
    },
    "Net.Amount": {
      "type": ["number", ""],
      "description": "Net amount (excluding VAT); No currency symbol"
    },
    "Vat.Amount": {
      "type": ["number", ""],
      "description": "VAT amount for that group; No currency symbol"
    }
  },
  "required": [
    "GrandTotal.Amount",
    "Invoice.Date",
    "Sender.VatId",
    "Vat.Rate",
    "Net.Amount",
    "Vat.Amount"
  ],
  "additionalProperties": false
}
